import React from 'react';
import RegistrationForm from '../Components/RegistrationForm';

const Registration = () => {
    return (
        <div className="relative w-full min-h-screen px-6 pb-20 pt-10 text-white font-body overflow-hidden bg-black">
        <RegistrationForm />
        </div>
    );
};

export default Registration;